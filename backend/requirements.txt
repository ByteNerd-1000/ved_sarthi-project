fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.8.0
sqlalchemy>=2.0.23
aiosqlite>=0.19.0
python-dotenv>=1.0.0
python-multipart>=0.0.6
pydantic-settings>=2.1.0
aiofiles>=23.2.1
groq>=0.4.0
pytest>=7.4.3
pytest-asyncio>=0.21.1
# Optional: online translation support
# googletrans==4.0.0-rc1    # commented out because it conflicts with groq's httpx requirement
# Note: Groq API is optional - falls back to offline mode if not available
# All functionality works offline without internet connection
